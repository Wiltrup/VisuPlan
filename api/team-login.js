const SUPABASE_URL='https://fzrtvogirhmnbicdaffc.supabase.co',SUPABASE_KEY='sb_publishable_oHmuwX8xm8d-77XLapdBFw_ragbZH4F';
async function data(response){const text=await response.text();return text?JSON.parse(text):null}
async function service(path,secret,options={}){const r=await fetch(`${SUPABASE_URL}${path}`,{...options,headers:{apikey:secret,Authorization:`Bearer ${secret}`,'Content-Type':'application/json',...(options.headers||{})}});if(!r.ok)throw new Error(await r.text());return data(r)}
module.exports=async function handler(request,response){if(request.method!=='POST')return response.status(405).json({error:'Kun POST er tilladt.'});const secret=process.env.SUPABASE_SECRET_KEY;if(!secret)return response.status(503).json({error:'Loginfunktionen er ikke klar.'});try{const {slug,action,password,email}=request.body||{};if(slug!=='team-2')return response.status(404).json({error:'Teamet blev ikke fundet.'});const teams=await service(`/rest/v1/teams_registry?slug=eq.${encodeURIComponent(slug)}&select=*`,secret),team=teams?.[0];if(!team?.editor_user_id)return response.status(400).json({error:'Personaleloginnet er ikke koblet korrekt.'});const user=await service(`/auth/v1/admin/users/${team.editor_user_id}`,secret);if(action==='login'){const login=await fetch(`${SUPABASE_URL}/auth/v1/token?grant_type=password`,{method:'POST',headers:{apikey:SUPABASE_KEY,Authorization:`Bearer ${SUPABASE_KEY}`,'Content-Type':'application/json'},body:JSON.stringify({email:user.email,password:String(password||'')})});const result=await data(login);if(!login.ok)return response.status(401).json({error:'Forkert personalekode.'});return response.status(200).json(result)}if(action==='recover'){if(String(email||'').trim().toLowerCase()!==String(team.recovery_email||'').toLowerCase())return response.status(200).json({ok:true});const recover=await fetch(`${SUPABASE_URL}/auth/v1/recover?redirect_to=${encodeURIComponent('https://visuplanner.dk/team-2')}`,{method:'POST',headers:{apikey:SUPABASE_KEY,Authorization:`Bearer ${SUPABASE_KEY}`,'Content-Type':'application/json'},body:JSON.stringify({email:user.email})});if(!recover.ok)throw new Error(await recover.text());return response.status(200).json({ok:true})}return response.status(400).json({error:'Ukendt handling.'})}catch(error){console.error(error);return response.status(500).json({error:'Loginhandlingen mislykkedes.'})}};
