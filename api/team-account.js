const SUPABASE_URL='https://fzrtvogirhmnbicdaffc.supabase.co',SUPABASE_KEY='sb_publishable_oHmuwX8xm8d-77XLapdBFw_ragbZH4F';
async function parse(response){const text=await response.text();return text?JSON.parse(text):null}
async function service(path,secret,options={}){const response=await fetch(`${SUPABASE_URL}${path}`,{...options,headers:{apikey:secret,Authorization:`Bearer ${secret}`,'Content-Type':'application/json',...(options.headers||{})}});if(!response.ok)throw new Error(await response.text());return parse(response)}
module.exports=async function handler(request,response){const secret=process.env.SUPABASE_SECRET_KEY;if(!secret)return response.status(503).json({error:'Kontofunktionen er ikke klar endnu.'});if(request.method!=='POST')return response.status(405).json({error:'Kun POST er tilladt.'});const authorization=request.headers.authorization||'';if(!authorization.startsWith('Bearer '))return response.status(401).json({error:'Log ind som personale.'});const userResponse=await fetch(`${SUPABASE_URL}/auth/v1/user`,{headers:{apikey:SUPABASE_KEY,Authorization:authorization}});if(!userResponse.ok)return response.status(401).json({error:'Personalesessionen er udløbet.'});const user=await parse(userResponse);const teams=await service(`/rest/v1/teams_registry?editor_user_id=eq.${encodeURIComponent(user.id)}&select=*`,secret);const team=teams?.[0];if(!team)return response.status(403).json({error:'Brugeren er ikke koblet til et team.'});try{const {action,value}=request.body||{};if(action==='reset-viewer'){if(String(value||'').length<6)return response.status(400).json({error:'Tavlekoden skal have mindst seks tegn.'});await service(`/auth/v1/admin/users/${team.viewer_user_id}`,secret,{method:'PUT',body:JSON.stringify({password:String(value)})});return response.status(200).json({ok:true})}return response.status(400).json({error:'Ukendt handling.'})}catch(error){console.error(error);return response.status(500).json({error:'Koden kunne ikke ændres.'})}};
