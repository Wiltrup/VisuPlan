const form=document.getElementById('onboardingForm'),statusEl=document.getElementById('onboardingStatus');
form.addEventListener('submit',async event=>{event.preventDefault();const button=form.querySelector('button[type="submit"]');button.disabled=true;button.textContent='Sender…';statusEl.textContent='';const body=Object.fromEntries(new FormData(form));body.resident_count=Number(body.resident_count);try{const response=await fetch('/api/public-request',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({type:'onboarding',...body})});const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.error||'Forespørgslen kunne ikke sendes.');form.reset();statusEl.className='finder-message success-message';statusEl.textContent='Tak! Din forespørgsel er sendt. Du vil blive kontaktet på den angivne mail.'}catch(error){statusEl.className='finder-message error-message';statusEl.textContent=error.message}finally{button.disabled=false;button.textContent='Send forespørgsel'}});
